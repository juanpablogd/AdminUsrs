@model IEnumerable<NSPecor.Models.MUB_TENSION>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Listado de TENSIÓN</h2>
<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MUB_NIVEL_TENSION.DESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MUB_NIVEL_TENSION.DESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_TENSION }) |
            @Html.ActionLink("Detalle", "Details", new { id=item.ID_TENSION }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.ID_TENSION })
        </td>
    </tr>
}

</table>
